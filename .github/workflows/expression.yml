name: expression
on: [push]
jobs:
  ifExpressionJob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
      - run: node client.js
        if: ${{ env.env1 }}
        env: 
          env1: ${{ true }}
          env2: hello
